{
	"name": "logifier",
	"version": "1.0.0",
	"scripts": {
		"client-app": "cd client && npm run start-app",
		"client-browser": "cd client && npm run start-browser",
		"server": "cd server && nodemon app.js",
		"dev": "concurrently --kill-others-on-fail \"npm server\" \"npm client\""
	},
	"dependencies": {
		"axios": "^0.21.1",
		"body-parser": "^1.18.3",
		"cors": "^2.8.5",
		"express": "^4.16.4",
		"multer": "^1.4.2",
		"nodemon": "^2.0.7"
	},
	"devDependencies": {
		"concurrently": "^4.0.1"
	}
}
